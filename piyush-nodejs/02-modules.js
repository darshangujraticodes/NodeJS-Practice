function add(a, b) {
  return a + b;
}

function subtract(a, b) {
  return a - b;
}

// commonjs export format
module.exports = { add, subtract };

// ES6 export format
// export default { add, subtract };
